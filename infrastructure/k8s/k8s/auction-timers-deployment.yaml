apiVersion: apps/v1
kind: Deployment
metadata:
  name: auction-timers
spec:
  replicas: 1
  selector:
    matchLabels:
      app: auction-timers
  template:
    metadata:
      labels:
        app: auction-timers
    spec:
      containers:
      - name: auction-timers
        image: auctionflow/auction-timers:latest
        ports:
        - containerPort: 8080
        env:
        - name: SPRING_CONFIG_LOCATION
          value: "file:/app/config/application.yml"
        envFrom:
        - secretRef:
            name: auction-timers-secret
        resources:
          requests:
            cpu: 250m
            memory: 256Mi
          limits:
            cpu: 500m
            memory: 512Mi
        volumeMounts:
        - name: config-volume
          mountPath: /app/config
      volumes:
      - name: config-volume
        configMap:
          name: auction-timers-config