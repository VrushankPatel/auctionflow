apiVersion: apps/v1
kind: Deployment
metadata:
  name: auction-analytics
spec:
  replicas: 1
  selector:
    matchLabels:
      app: auction-analytics
  template:
    metadata:
      labels:
        app: auction-analytics
    spec:
      containers:
      - name: auction-analytics
        image: auctionflow/auction-analytics:latest
        ports:
        - containerPort: 8080
        env:
        - name: SPRING_CONFIG_LOCATION
          value: "file:/app/config/application.yml"
        envFrom:
        - secretRef:
            name: auction-analytics-secret
        resources:
          requests:
            cpu: 250m
            memory: 256Mi
          limits:
            cpu: 500m
            memory: 512Mi
        volumeMounts:
        - name: config-volume
          mountPath: /app/config
      volumes:
      - name: config-volume
        configMap:
          name: auction-analytics-config